;; Num-Navigation layer:
;;  - should inherit from NumPad
;;  - left: one-handed shortcuts (Cmd/Ctrl-AZXCV) + Tab/S-Tab
;;  - top: Super-num (i3/sway) or Alt-num (browser)

;; The `lrld` action stands for "live reload". This will re-parse everything
;; except for linux-dev, i.e. you cannot live reload and switch keyboard devices.


(deflayer navigation
  M-1   M-2   M-3   M-4   M-5   M-6   M-7   M-8   M-9   XX    XX   lrld
  caps  home  up    end   pgup  XX    @-    @b7   @b8   @b9   @*
  tab   lft   down  rght  pgdn  XX    @+    @b4   @b5   @b6   @b00
  bck   prtsc esc   ins   fwd   XX    XX    @.    @b1   @b2   @b3   @,
              C-bspc            _                 del
)

(deflayer funckeys
  _     _     _     _     _     _     _     _     _     _     _    lrld
  @ndo  @cpy  @cut  @pst  _     XX    XX    F7    F8    F9    F10
  lsft  lmet  lctl  lalt  @kz+  XX    XX    F4    F5    F6    F11
  _     vold  mute  volu  @kz-  XX    XX    XX    F1    F2    F3   F12
              _                 _                 @mou
)

(deflayer mousectl
  _     _     _     _     _     _     _     _     _     _     _    lrld
  XX    XX    @mwu  XX    XX    XX    XX    mltp  @mau  mrtp  XX
  XX    @slw  @med  @fst  XX    XX    XX    @mal  @mad  @mar  mlft
  XX    @mwl  @mwd  @mwr  XX    XX    XX    XX    mltp  mmtp  mrtp XX
              _                mlft               _
)


(defalias
  b00  (tap-hold $tap_timeout $long_hold_timeout @b0 lsft)
  func (layer-toggle funckeys)
  kz- C-S-kp- ;; kitty zoom out
  kz+ C-S-=   ;; kitty zoom in

;;  mou (layer-toggle mousectl) ;; defined in base_lt_hrm.kbd
  mwu (mwheel-up   50 120)
  mwd (mwheel-down 50 120)
  mwl (mwheel-left 50 120)
  mwr (mwheel-right 50 120)
  mau (movemouse-accel-up    4 500 1 10)
  mad (movemouse-accel-down  4 500 1 10)
  mal (movemouse-accel-left  4 500 1 10)
  mar (movemouse-accel-right 4 500 1 10)
  slw (movemouse-speed 10)
  med (movemouse-speed 50)
  fst (movemouse-speed 200)
)

;; vim: set ft=lisp

