;; Num-Navigation layer:
;;  - should inherit from NumPad
;;  - left: one-handed shortcuts (Cmd/Ctrl-AZXCV) + Tab/S-Tab
;;  - top: Super-num (i3/sway) or Alt-num (browser)

;; The `lrld` action stands for "live reload". This will re-parse everything
;; except for linux-dev, i.e. you cannot live reload and switch keyboard devices.

(deflayer navigation
  M-1  M-2  M-3  M-4  M-5  lrld M-6  M-7  M-8  M-9  M-0
  caps home up   end  pgup      @-   @7   @8   @9   @/
  tab  lft  down rght pgdn      @+   @4   @5   @6   @0
  bck  prtsc esc ins  fwd   _   @.   @1   @2   @3   @,
            @func           _             del       @0
)

(deflayer funckeys
  _    _    _    _    _    lrld _    _    _    _    _
  _    _    _    _    _         _    F7   F8   F9   F10
  _    lsft lmet lctl lalt      _    F4   F5   F6   F11
  _    _    _    _    _    _    _    F1   F2   F3   F12
            _              _              _         _
)

(defalias
	func (layer-toggle funckeys)
)
;; vim: set ft=lisp
